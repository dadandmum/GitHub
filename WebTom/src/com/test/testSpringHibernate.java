package com.test;

public class testSpringHibernate {
	public static void main(String[] args) {
/*
		 BeanFactory beansfactory = new ClassPathXmlApplicationContext("ApplicationContext.xml");
		 System.out.println(beansfactory);
		 HibernateUtil hibernateUtil = (HibernateUtil)beansfactory.getBean("hibernateUtil");

			Session session = HibernateUtil.getSession();
	        Transaction tx = session.beginTransaction();
	        UserBase uBase = new UserBase("tata","333");
	        session.save(uBase);
	        tx.commit();
	        HibernateUtil.closeSession();*/
	}
}
